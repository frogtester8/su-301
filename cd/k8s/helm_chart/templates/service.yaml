apiVersion: v1
kind: Service
metadata:
  name: {{ include "myJavaWebApp.fullname" . }}
  labels:
    {{- include "myJavaWebApp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "myJavaWebApp.selectorLabels" . | nindent 4 }}
